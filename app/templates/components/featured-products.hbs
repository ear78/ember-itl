<div class="row">
    <div class="small-12 medium-6 columns">
        <img src="{{product.image}}" alt="">
    </div>
    <div class="small-12 medium-6 columns">
        <h2>{{product.title}}</h2>
        <h5>{{product.description}}</h5>
        <button class="button radius" type="button" name="button">Learn More</button>
    </div>
</div>
